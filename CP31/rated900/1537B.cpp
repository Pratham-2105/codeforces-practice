#include <bits/stdc++.h>
#include <climits>
#include <cmath>
using namespace std;

using ll = long long;

void solve() {
  ll n, m, i, j;
  cin >> n >> m >> i >> j;

  cout << 1 << " " << 1 << " " << n << " " << m << "\n";
  cout.flush();
  return;
}

int main() {

  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t = 1;
  cin >> t;

  while (t--) {
    solve();
  }
}
