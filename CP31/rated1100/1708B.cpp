/*
 *
 *  Author: beingPratham
 *
 */

#include <bits/stdc++.h>
#include <linux/limits.h>
#include <vector>
using namespace std;

using ll = long long;

void solve() {
  ll n, l, r;
  cin >> n >> l >> r;

  int ok = 1;

  for (int i = 1; i <= n; ++i) {
  }

  return;
}

int main() {

  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t = 1;
  cin >> t;

  while (t--) {
    solve();
  }
}
